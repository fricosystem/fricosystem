import { useEffect } from 'react';

/**
 * Hook desativado temporariamente para evitar interferência com Vite HMR
 * A funcionalidade foi removida para garantir que não interfere com o ambiente de desenvolvimento
 */
export const usePostMessageFix = () => {
  // Hook desativado - não faz nada
  // A correção para DataCloneError foi temporariamente removida
  // para resolver problemas com o Vite HMR
  return;
};
